- if coach_signed_in?
  %p#notice= notice
  %p
    = @pitch.address
  %p
    = @pitch.players.map {|c| c.email}.join(', ')

  = link_to 'Edit', edit_pitch_path(@pitch)
  \|
  = link_to 'Back', pitches_path
