- if coach_signed_in?
  %p#notice= notice
  %p
    = @district.name
  %p
    = @district.players.map {|c| c.email}.join(', ')

  = link_to 'Edit', edit_district_path(@district)
  \|
  = link_to 'Back', districts_path
